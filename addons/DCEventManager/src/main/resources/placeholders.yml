events:
  AddonDisableEvent:
    placeholders:
      - name: addon  # placeholder id
        replace: "%addon%" # replaced placeholder
        method: "getAddon#getName" # class method
      - name: reason
        replace: "%reason%"
        method: "getReason"
      - name: caused
        replace: "%caused%"
        method: "getCaused#getName"
  AddonEnableEvent:
    placeholders:
      - name: addon  # placeholder id
        replace: "%addon%" # replaced placeholder
        method: "getAddon#getName" # class method
      - name: reason
        replace: "%reason%"
        method: "getReason"
      - name: caused
        replace: "%caused%"
        method: "getCaused#getName"
  AnimationStartEvent:
    placeholders:
      - name: player
        replace: "%player%"
        method: "activeCase#player#getName"
